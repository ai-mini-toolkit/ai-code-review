# Production profile configuration
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.aicodereview: INFO
    org.springframework.web: WARN

# Database configuration (to be filled in Story 1.3)
# spring:
#   datasource:
#     url: ${DATABASE_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}

# Redis configuration (to be filled in Story 1.4)
# spring:
#   data:
#     redis:
#       host: ${REDIS_HOST}
#       port: ${REDIS_PORT:6379}
